import React, { Component } from 'react';

class PrintPreviewScreen extends Component {
    componentDidMount() {
        // console.log(this.props.PrintPreviewState);
    }

    render() {
        return <>Print Preview</>;
    }
}

export default PrintPreviewScreen;
